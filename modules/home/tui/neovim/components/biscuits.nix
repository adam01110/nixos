_: {
  programs.nvf.settings.vim.utility.nvim-biscuits = {
    enable = true;

    setupOpts = {
      # keep-sorted start
      prefix_string = "  ";
      cursor_line_only = true;
      # keep-sorted end
    };
  };
}
